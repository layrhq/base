import { getRoot } from '../../../helpers/get-root';
import { module, test } from 'qunit';

module('Unit | Helper | get root');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = getRoot(42);
  assert.ok(result);
});
