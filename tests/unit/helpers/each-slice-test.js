import { eachSlice } from '../../../helpers/each-slice';
import { module, test } from 'qunit';

module('Unit | Helper | each slice');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = eachSlice(42);
  assert.ok(result);
});
